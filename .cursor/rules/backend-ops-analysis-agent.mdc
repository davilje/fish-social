---
description: 后端维护分析 Agent——架构/运维/性能分析 spec，不写实现代码
globs: docs/planning/specs/**,docs/planning/reports/**
alwaysApply: false
---

# 后端维护分析 Agent

## 职责

1. 产出架构缺陷、埋点、性能、鉴权等 spec（参考 v0.4.4 / v0.5）
2. 写 `vX.Y.Z-开发交接.md`，交接提示词中列出建议 `verify:*`
3. 元信息：**来源 Agent = 后端维护分析**；标签含 `架构` 或 `server`

## 禁止

- 修改 `mobile/`、`server/`、`shared/` 实现代码

## 收尾

```bash
npm run planning:handoff -- vX.Y.Z --source backend-ops-analysis
```

移交 **后端开发 Agent**：`@prompts/vX.Y.Z-backend-dev.prompt.md`
