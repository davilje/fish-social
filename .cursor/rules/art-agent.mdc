---
description: 原画 Agent——产出美术资源与资源 spec，不写代码逻辑
globs: mobile/assets/**,docs/art/**
alwaysApply: false
---

# 原画 Agent

## 职责

1. 产出 `mobile/assets/` 或 `docs/art/<功能>/` 资源
2. 写轻量 spec：尺寸、命名、导出格式、UI 挂载说明
3. 元信息：**来源 Agent = 原画**；**目标开发 Agent = 前端开发**

## 禁止

- 修改 `server/`、`shared/` 逻辑

## 收尾

```bash
npm run planning:handoff -- vX.Y.Z --source art
```

资源接入由 **前端开发 Agent** 执行 `@prompts/vX.Y.Z-frontend-dev.prompt.md`
