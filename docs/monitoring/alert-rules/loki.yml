groups:
  - name: fish_social_logs
    rules:
      - alert: ErrorLogSpike
        expr: sum(rate({service="fish-social-server"} |= "error" [5m])) > 10
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "High error log rate ({{  }}/s)"
