<!-- 来源: docs/planning/specs/v0.4.4-未完成项补完.md -->
<!-- 用途: 补齐 v0.4.4 剩余缺口 -->

你是 Fish Social 后端开发 Agent。补齐 v0.4.4 当前未完成的 3 个点。

必读：

- `docs/planning/specs/v0.4.4-未完成项补完.md`
- `docs/planning/specs/服务器架构缺陷与埋点设计-v0.4.4.md`

## 任务 1（P1，必须）

实现 `phase_transition_invalid` 检测与日志：

1. 建立合法迁移白名单（含 disconnected 重连特例）
2. 在统一 `transitionPhase` 路径检测非法迁移
3. 产出结构化日志 `[phase_transition_invalid]`
4. 写入 `recordFishingMetric('phase_transition_invalid', ...)`
5. timeline summary 增加 invalid 计数

要求：非法迁移只告警不阻断流程。

## 任务 2（P2，必须）

补齐性能埋点：

1. `tick_fishing_phases_duration_ms`
2. `bite_check_loop_duration_ms`
3. `ecology_tick_duration_ms`
4. `socket_broadcast_fanout`
5. `slow_query`
6. `admin_route_duration_ms` + `admin_route_slow`

要求：

- 使用结构化日志
- 字段至少包含 `eventType`, `durationMs`, `ts`
- 慢阈值可配置（默认值可放常量）

## 任务 3（P1，必须）

新增 `pending_catch_expired` 真实超时慢测 verify：

1. 创建 pending catch
2. 不 accept，等待真实 `PENDING_CATCH_TIMEOUT_MS + buffer`
3. 断言产出 `pending_catch_expired`
4. 断言鱼锁释放

建议：

- 新增 `scripts/verify-pending-timeout.ts`
- npm script：`verify:pending-timeout`
- 支持 `--slow` 才跑真实等待，默认快速模式 skip

## 不改

- 不做多实例改造
- 不做持久化在线态改造
- 不改核心钓鱼概率公式

## 完成后

1. 更新 `v0.4.4-未完成项补完.md` 状态为已实现
2. 补 CHANGELOG 实现项
3. 提供运行结果（含慢测命令输出摘要）

## commit 建议

```text
feat(observability): complete v0.4.4 remaining metrics and slow verify

Add invalid phase transition detection, performance observability logs,
and a real timeout verify for pending_catch_expired.
```
