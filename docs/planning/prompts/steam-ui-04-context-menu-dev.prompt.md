# STEAM-UI-04：桌面宠物右键菜单

在 `fish-social-unity/` 内实现 Fish Social 产品区域的右键菜单。

菜单项：

```text
当前鱼塘 / 好友与聊天 / 鱼获与背包 / 图鉴 / 设置 / 隐藏到托盘 / 退出
```

要求：
- 只作用于 Fish Social 窗口或宠物区域。
- 不拦截 Windows 桌面其他区域的系统右键。
- 菜单视图和业务操作通过接口连接。
- 菜单操作不得触发 `leave_pond`。

验收：
- 右键打开、点击分发、重复打开和关闭均正常。
- 托盘和退出调用现有桌面生命周期服务。
