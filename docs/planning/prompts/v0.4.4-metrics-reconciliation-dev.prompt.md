<!-- 来源: docs/planning/specs/v0.4.4-埋点缺口复核与补全.md -->
<!-- 用途: 埋点表与代码对齐 — v0.4.4-patch2 -->

你是 Fish Social **后端开发 Agent**。按策划文档补齐埋点缺口，使代码与 `docs/planning/reports/v0.4.4-埋点表清单.xlsx` 对齐。

## 必读

1. `docs/planning/specs/v0.4.4-埋点缺口复核与补全.md`（需求与决策）
2. `docs/planning/reports/v0.4.4-埋点表清单.xlsx`（33 条埋点标准；**更新状态列时保持现有列顺序与必填字段换行格式**）
3. `docs/planning/specs/服务器架构缺陷与埋点设计-v0.4.4.md`（字段规范）

## 背景摘要

| 类型 | 事件 |
|------|------|
| 真缺失 | `session_rebound`, `snapshot_build_duration_ms`, `sqlite_query_slow` |
| 别名未对齐 | 表 `socket_disconnect` ↔ 代码 `disconnect`；表 `pending_catch_accept` ↔ 代码 `catch_accept` |
| 字段缺口 | `slow_query` 缺 `rows` |
| xlsx 滞后 | 8 条性能/invalid 已实现但状态仍「待开发」 |

---

## 任务 1（P1）`session_rebound` 结构化日志

文件：`server/src/index.ts`

1. **`register_player`**：当 `playerSockets.set(playerId, socket.id)` 且存在 `previousSocketId !== socket.id` 时：
   - `logStructuredEvent('session_rebound', 'session_rebound', { playerId, socketId, previousSocketId, reason: 'register_player' })`

2. **`join_pond` reconnect 成功分支**（`socketByUserId.set` 之前）：
   - `logStructuredEvent('session_rebound', 'session_rebound', { playerId, userId, socketId, pondId, reason: 'join_pond_reconnect' })`

不要求写入 `fishing_metrics`（表内类型为结构化日志）。

---

## 任务 2（P1）eventType 对齐 + summary 兼容

### 2.1 `socket_disconnect`

- `server/src/fishingStateMachine.ts` `handleDisconnect`：将 `recordFishingMetric('disconnect', ...)` 改为 **`socket_disconnect`**
- `server/src/fishingMetrics.ts`：
  - `FishingMetricEvent` 增加 `'socket_disconnect'`（可保留 `'disconnect'` 只读兼容）
  - summary `switch`：`case 'disconnect':` 与 `case 'socket_disconnect':` **合并计入** `disconnectCount`

### 2.2 `pending_catch_accept`

- `server/src/index.ts` `accept_catch`：将 `recordFishingMetric('catch_accept', ...)` 改为 **`pending_catch_accept`**
- payload 补全 **`sizeM: result.item.sizeM`**
- summary：`catch_accept` 与 `pending_catch_accept` 合并计入 catch 相关计数（若有独立字段则同步）

---

## 任务 3（P2）`snapshot_build_duration_ms`

文件：`server/src/gameState.ts`（推荐在 `buildSnapshot` 内）

1. 计时 `buildSnapshot` 执行耗时
2. 使用与 `index.ts` 相同的 **`PERF_LOG_INTERVAL_MS` 节流**（可抽常量到 `fishingObservability.ts` 或 `runtimeConfig`）
3. 节流命中时：`logStructuredEvent('perf', 'snapshot_build_duration_ms', { eventType, durationMs, pondId, userCount, ts })`

注意：`buildSnapshot` 调用频繁，**禁止每条都打日志**。

---

## 任务 4（P2）`sqlite_query_slow` + `rows` 字段

文件：`server/src/db.ts` `timedDbQuery`

1. 超阈值时，若 `result` 为数组，设置 `rows: result.length`
2. 日志 JSON 的 `eventType` 使用 **`sqlite_query_slow`**（表内 canonical）
3. 保留 `[slow_query]` 作为 **console 前缀** 或第二行兼容日志（便于现有 grep）

---

## 任务 5 验收脚本

1. 扩展 `scripts/verify-server-observability.ts`：
   - 覆盖 `phase_transition_invalid`（已有则保持）
   - 可选：轻量检测 `session_rebound` 日志路径（mock 或文档断言）
   - 文件存在性断言：`verify-pending-timeout.ts` + `package.json` script `verify:pending-timeout`

2. 本地跑通：
   ```bash
   npm run verify:server-observability
   npm run verify:pending-timeout          # 快测 SKIP 即可
   npm run verify:pending-timeout -- --slow  # 可选，真实等待
   ```

---

## 任务 6 同步埋点表 xlsx

编辑 `docs/planning/reports/v0.4.4-埋点表清单.xlsx`「状态」列（**不要改列顺序/换行格式**）：

| 事件名 | 新状态 |
|--------|--------|
| `phase_transition_invalid` | 已实现 |
| `tick_fishing_phases_duration_ms` | 已实现 |
| `bite_check_loop_duration_ms` | 已实现 |
| `ecology_tick_duration_ms` | 已实现 |
| `socket_broadcast_fanout` | 已实现 |
| `slow_query` | 已实现 |
| `admin_route_duration_ms` | 已实现 |
| `admin_route_slow` | 已实现 |
| `session_rebound` | 已实现 |
| `snapshot_build_duration_ms` | 已实现 |
| `sqlite_query_slow` | 已实现 |
| `socket_disconnect` | 已实现（去掉「兼容 disconnect」） |
| `pending_catch_accept` | 已实现（去掉「兼容 catch_accept」） |
| `pending_catch_expired(真实超时用例)` | 已实现(慢测需 --slow) |

---

## 不改

- 多实例、在线态持久化、钓鱼公式、状态机白名单逻辑
- 不为 `bite_hook` 等表外事件补 xlsx 行

---

## 完成后

1. 将 `docs/planning/specs/v0.4.4-埋点缺口复核与补全.md` 状态改为 **已实现**
2. 更新 `docs/planning/CHANGELOG.md` 策划 + 实现条目
3. 回复中附：`verify:server-observability` 输出摘要、改动文件列表

## commit 建议

```text
feat(observability): reconcile metrics with v0.4.4 tracking table

Add session_rebound and snapshot_build_duration_ms logs, align
socket_disconnect/pending_catch_accept event types, emit
sqlite_query_slow with rows, and sync xlsx status column.
```
