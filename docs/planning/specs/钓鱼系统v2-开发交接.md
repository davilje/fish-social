# 钓鱼系统 v2 — 开发交接（批量）

> 策划已完成，全部子 spec 状态：**已实现**。  
> 请 **新开开发 Agent 对话**，按阶段顺序实现，每阶段合并前自检对应 spec §验收标准。

---

## 必读（按顺序）

1. [specs/README.md](./README.md) — 阶段索引与决策速查
2. [specs/A0-数值重构.md](./A0-数值重构.md) — **先做**
3. [specs/A1-飘字广播.md](./A1-飘字广播.md) — 依赖 A0
4. [specs/A2-Debug面板.md](./A2-Debug面板.md) — 依赖 A0，可与 A1 并行
5. [specs/B0-商店基础.md](./B0-商店基础.md) — A 期灰度后
6. [specs/B1-鱼饵偏好.md](./B1-鱼饵偏好.md) — 依赖 B0
7. [specs/状态机需求描述.md](./状态机需求描述.md) — **仅 C6**，A/B 期不做
8. [product/v0.1.0-功能全景.md](../product/v0.1.0-功能全景.md) — 基线对照
9. [WORKFLOW.md](../WORKFLOW.md) §4 — 源码索引

---

## 交接提示词（复制给开发 Agent）

```
你是 Fish Social 项目的开发 Agent。本次只写代码，策划文档已就绪。

## 范围与顺序（严格遵守）
按阶段串行交付，每阶段单独 PR/合并，不要一次做完 B/C：

1. **A0** — docs/planning/specs/A0-数值重构.md（P0，无依赖）
2. **A1** — docs/planning/specs/A1-飘字广播.md（依赖 A0）
3. **A2** — docs/planning/specs/A2-Debug面板.md（依赖 A0；可与 A1 并行）
4. 暂停灰度 / 自测后，再做 **B0** → **B1**
5. **C 期**（C1~C7）仅在用户明确要求时做；**C6 状态机**见 状态机需求描述.md，A 期禁止实现 baiting/casting/hooked 等完整状态机

## 核心约束
- A0：指数咬钩 `P = 1 - exp(-W * 0.02)`；品质终生不变；新建 shared/fishing.ts
- A0：不改动客户端（mobile 可忽略新字段）；保留 fish_bite/fish_miss 事件名
- A1：仅飘字；封装 emitFishingFloatText() 供 C6 复用
- A2：GET /api/admin/ponds/:pondId/fishing-debug + Admin UI 面板
- B0：player_gear 表；A0 无状态机时扣饵临时口径 = 咬钩判定成功时扣
- B1：baitBiteBonus(baitId, speciesId) + diet 偏好
- 禁止：完整 fishingPhase 状态机、阶段动画（归 C6）

## 涉及核心文件
- shared/fishing.ts（新建）、fish.ts、pondEcology.ts、constants.ts
- server/fishingSession.ts（新建）、pondEcology.ts、index.ts、bots.ts、admin.ts
- server/migrations/（A0 迁移）
- mobile：A1 FishingFloatText.tsx；A2 AdminFishingDebugPanel.tsx；B0 商店 UI

## 每阶段完成后必须
- [ ] 自检该 spec §验收标准全部勾选
- [ ] spec 元信息「状态」→ **已实现**
- [ ] 更新 docs/planning/CHANGELOG.md
- [ ] A 期全部完成后回写 docs/planning/product/ 或新建 v0.2.0 全景章节

## 禁止
- 扩大需求（尤其 C6 状态机提前做）
- 修改登录/认证架构
- 在无 spec 或状态非「已确认」时开工
```

---

## 阶段验收门禁

| 阶段 | 合并前必过 |
|------|------------|
| A0 | 迁移 dry-run；品质 tick 不变；Bot 不卡死；旧客户端可收 fish_bite |
| A1 | 8 人同塘飘字 <500ms；escape 全塘广播 |
| A2 | debug tickBiteChance 误差 <1pp；401 无 key |
| B0 | 幂等购买；饵耗尽回退 basic |
| B1 | corn/live 对 herbivore/carnivore 加成符合表格 |

---

## 策划侧登记

| 字段 | 内容 |
|------|------|
| 功能名称 | 钓鱼系统 v2（A0~B1 + C 按需） |
| Spec 路径 | `docs/planning/specs/README.md` 及子文档 |
| 目标版本 | v0.2.0-alpha（A）→ v0.2.1（B） |
| Spec 状态 | **已实现**（交接完成） |
| 策划完成日期 | 2026-07-01 |
