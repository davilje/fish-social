# v0.3.2 开发交接：鱼群补充优化（动态频率 · 品质缺口）

**策划文档**：`docs/planning/specs/生态调优-咬钩间隔与鱼群恢复.md` §3.10–§3.11（D7、D8）  
**状态**：**已实现**
**前置**：v0.3.1 已实现（固定 15min 补充 + `rollFishQuality()`）

---

你是 Fish Social 开发 Agent。在 **不改动** D1–D6 的前提下，实现 D7、D8。

## 产品决策（必守）

1. **D7**：补充**检查间隔**随塘内活跃钓鱼人数变化；**单次补充条数公式不变**（§3.3）
2. **D8**：仅**系统补充**走品质缺口权重；`seedPond` 仍 `rollFishQuality()`
3. **鱼数 = 0** 强制兜底：`force=true` 跳过 D7 间隔，仍立即补鱼
4. 活跃人数：**非 Bot** 且 `isFishingActive(fishingPhase)`，与状态机定义一致
5. 理想品质比例 = `FISH_QUALITIES` 权重（38/28/18/9/4/2/1），不另存 DB 快照

---

## 任务 1 — shared 常量与纯函数

**文件**：`shared/pondEcology.ts`（或 `shared/fishing.ts` 若更易测）

### 1.1 D7 常量

```typescript
export const POND_SUPPLEMENT_FULL_ACTIVITY_ANGLERS = 3;
export const POND_SUPPLEMENT_IDLE_INTERVAL_STEP = 0.5;
export const POND_SUPPLEMENT_MAX_INTERVAL_MULT = 3.0;

export function calcSupplementCheckMs(activeAnglers: number): number {
  const shortfall = Math.max(0, POND_SUPPLEMENT_FULL_ACTIVITY_ANGLERS - activeAnglers);
  const mult = Math.min(
    POND_SUPPLEMENT_MAX_INTERVAL_MULT,
    1 + shortfall * POND_SUPPLEMENT_IDLE_INTERVAL_STEP,
  );
  return Math.floor(POND_SUPPLEMENT_CHECK_MS * mult);
}
```

### 1.2 D8 常量与函数

```typescript
export const SUPPLEMENT_HIGH_TIER_PRESENT_MULT = 0.02;
export const SUPPLEMENT_MID_TIER_SATURATION_RATIO = 0.6;
export const SUPPLEMENT_MID_TIER_PRESENT_MULT = 0.15;
export const SUPPLEMENT_LOW_TIER_DEFICIT_BOOST = 1.25;
export const SUPPLEMENT_HIGH_TIER_MIN_INDEX = 3;
export const SUPPLEMENT_MID_TIER_INDEX = 2;

export function calcSupplementQualityWeights(
  actualByQuality: Record<FishQuality, number>,
  maxPopulation: number,
): number[];

export function rollSupplementQuality(
  actualByQuality: Record<FishQuality, number>,
  maxPopulation: number,
): FishQuality;
```

完整公式见策划 §3.11.4。`sum(weights)===0` 时 fallback `rollFishQuality()`。

导出到 `shared/index.ts`。

---

## 任务 2 — server 补充逻辑

**文件**：`server/src/pondEcology.ts`

1. 新增 `countActiveAnglers(pondId)` — 调 `listUsersInPond` + `isFishingActive`（可从 `fishingStateMachine` 导出 helper 或内联相同 phase 列表）
2. `trySupplement`：
   - `effectiveMs = calcSupplementCheckMs(countActiveAnglers(pondId))`
   - 非 `force` 时用 `effectiveMs` 比较 `last_supplement_at`
3. 补充前 `SELECT quality, COUNT(*) ... GROUP BY quality` 或使用 `listPondFishEntities` 聚合成 `actualByQuality`
4. `createSupplementFish`：品质改为 `rollSupplementQuality(counts, config.maxPopulation)`
5. 日志：`[pond supplement] ${pondId} +${n} active=${a} checkMs=${effectiveMs} ...`

---

## 任务 3 — Debug / Admin（可选但推荐）

**文件**：`server/src/fishingDebug.ts` 或 Admin API

在鱼塘 Debug 中增加：

- `activeAnglers`
- `effectiveSupplementCheckMs`
- 各品质 `actual / ideal`（ideal = baselineFrac × maxPopulation）

---

## 任务 4 — 测试

**文件**：`scripts/a0-verify.ts` 或 `shared` 旁单测

| 用例 | 期望 |
| ---- | ---- |
| `calcSupplementCheckMs(3)` | `900_000`（15min） |
| `calcSupplementCheckMs(0)` | `2_250_000`（37.5min；倍率上限 45min = `2_700_000`） |
| 灰0绿0、紫3仍在 | 紫权重 ≪ 灰权重 |
| 全空塘 | fallback 接近全局权重 |
| 紫 actual=0 | 紫权重 > 0 |

---

## 任务 5 — 模拟脚本（可选）

**文件**：`scripts/simulate-pond-day.ts`

- `trySupplement` 分支接入 D7/D8，重跑 1 人/塘 24h，对比补充品质分布

---

## 不改项

- `POND_SUPPLEMENT_BATCH_RATIO` / `BATCH_MAX` / `TARGET_RATIO`
- `rollJuvenileSize`、物种 `pickSpecies`
- 咬钩 300s、生长曲线、脱钩 ×1.02

---

## 提交建议

```
feat(ecology): v0.3.2 dynamic supplement interval and quality-deficit roll
```
