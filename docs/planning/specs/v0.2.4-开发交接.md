# v0.2.4 开发交接

> 策划已完成，状态：**已实现**（§6.3 Debug 统一面板，2026-07-03）。

---

## 元信息

| 字段 | 内容 |
|------|------|
| 目标版本 | v0.2.4 |
| 主 Spec | [数值重构v2-成长咬钩与文案.md](./数值重构v2-成长咬钩与文案.md) **§6.3** |
| 次 Spec | [UI体验修复-社交商店图鉴.md](./UI体验修复-社交商店图鉴.md) **§8**（若尚未验收） |
| 策划日期 | 2026-07-03 |

---

## 已实现（开发勿重复，仅验收）

| 项 | 说明 |
|----|------|
| A0-v2 数值核心 | `FISH_BITE_CHECK_MS=30s`、钓点乘法、`calcEffectiveBiteRate`、14 日成长、满尺寸 3%/98.4%/2h 等（见 `shared/fishing.ts`） |
| §8.1 图鉴「新」角标 | `PendingFishCatch.isCodexNew`、`CatchFishModal` 角标（验收：无 Alert） |
| UI-1 主体 | 纪念照、商店/图鉴背包式、Debug 入口、好友动态等（对照 UI spec §3 自检） |

---

## 待开发（本交接范围）

### 任务 1 — Debug 统一方格面板（P0，必做）

**Spec**：`数值重构v2` **§6.3**（含 §6.3.1~6.3.6）

**要求摘要**：

1. 合并 `PondFishGrid` + `AdminFishingDebugPanel` → **`AdminPondFishDebugGrid`**
2. 小页签：`[鱼塘] [钓位1] [钓位2] …`
3. 鱼格 **200×200 px**，字号 ≥ spec §6.3.2
4. **鱼塘页签**：全塘鱼，仅生态字段（无咬钩/脱钩/饵/点位）
5. **钓位页签**：仅 `effectiveBite > 0` 的鱼；展示咬钩率、脱钩率、鱼饵加成、运气 ×N
6. 角标 **「体型上限」** 替代「满」+ 底部图例
7. API：`spots[].fishContributions` 仅返回 `effectiveBite > 0`

**涉及文件**：

- `mobile/components/AdminPondFishDebugGrid.tsx`（新建）
- `mobile/app/admin.tsx`（删除双区块，改用合并组件）
- `mobile/components/AdminFishingDebugPanel.tsx`（删除或内联废弃）
- `server/src/fishingDebug.ts`（过滤 fishContributions）

**验收**：spec §6.3.6 + §9 文案与 Debug 全部勾选

---

### 任务 2 — 商店商品显示（P0，若未验收）

**Spec**：`UI体验修复` **§8.2**

- `ShopModal` 双栏、`width:100%`、`minHeight`、打开前 `refresh()`
- API 失败时 `BAITS`/`TACKLES` 静态兜底 + 重试

**验收**：§8.2.4

---

## 交接提示词（复制给开发 Agent）

```
你是 Fish Social 开发 Agent。只写代码，策划已确认。

## 必读（按顺序）
1. docs/planning/specs/v0.2.4-开发交接.md
2. docs/planning/specs/数值重构v2-成长咬钩与文案.md §6.3（Debug 面板，P0 必做）
3. docs/planning/specs/UI体验修复-社交商店图鉴.md §8.2（商店显示，若 §8.1/§3 已验收则只做 8.2）

## 任务 1：AdminPondFishDebugGrid（P0）
- 合并鱼塘明细 + 钓鱼概率为一个面板
- 页签：鱼塘（全鱼）| 钓位1…N（仅 effectiveBite>0）
- 每格 200×200，字号见 spec §6.3.2
- 鱼塘页签不显示咬钩/脱钩/饵/点位；钓位页签每条鱼显示这些字段
- 「满」→「体型上限」+ 图例
- fishingDebug API：fishContributions 过滤 effectiveBite>0

## 任务 2：ShopModal（若未做）
- 见 UI spec §8.2

## 禁止
- 扩大范围（不做 C6 新功能）
- 重复实现已有 isCodexNew / 30s tick / 钓点乘法

## 完成后
- [ ] 自检 spec §6.3.6、§9 Debug 验收项
- [ ] UI spec §8.2 验收（如本次包含）
- [ ] 更新 docs/planning/CHANGELOG.md
- [ ] 将相关 spec 状态标为「已实现」
```

---

## 门禁

| 任务 | 合并前必过 |
|------|------------|
| Debug 面板 | 200×200 格；钓位 tab 仅 effectiveBite>0；无「满」字 |
| 商店 | 4 饵 + 4 具可见、可选中 |
