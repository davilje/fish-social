# v0.3.1 开发交接：生态调优（咬钩间隔 · 鱼群补充 · 脱钩成长 · 绝对生长曲线）

**策划文档**：`docs/planning/specs/生态调优-咬钩间隔与鱼群恢复.md`  
**状态**：**已实现**

---

你是 Fish Social 开发 Agent。按下列任务实现，策划已确认。

## 必读

- `docs/planning/specs/生态调优-咬钩间隔与鱼群恢复.md`（完整设计与公式）

---

## 产品决策（必守）

1. **咬钩 tick = 300 秒**；生态 tick 仍 30 秒
2. **咬钩率数值表不改**；文案「每 300 秒 / 5 分钟」
3. **废弃阻断式耗尽恢复**；每 15 分钟 `trySupplement`（缺口 **50%**，上限 12 条）
4. **移除繁殖**：删除 `tryBreed` 及所有调用；鱼群仅播种 + 补充
5. **幼年尺寸**：绝对 **0.08~0.20m**，与品质无关（`rollJuvenileSize`）
6. **脱钩成长**：`newSize = sizeM × 1.02`（当前体长 +2%），非剩余空间
7. **生长曲线**：全鱼共用 `L(t)`，7 天 = 40m；品质仅作上限截断；**无 growth_multiplier**
8. **脱钩 & 收杆**：**仅体长 `sizeM`**，非线性曲线；40m→脱钩 **98.5%**、收杆 **2h**；与品质/物种无关

---

## 任务 7 — 体长脱钩率 & 收杆窗口（D6）

### 7.1 常量

```typescript
export const REFERENCE_SIZE_M = 40;
export const ESCAPE_AT_40M = 0.985;
export const SIZE_ESCAPE_CURVE_EXPONENT = 1.8;
export const HOOK_AT_40M_MS = 7_200_000;
export const HOOK_MIN_MS = 2_000;
export const SIZE_HOOK_CURVE_EXPONENT = 1.8;
```

### 7.2 脱钩（仅 sizeM）

```typescript
export function calcSizeEscapeRate(sizeM: number): number {
  const n = clamp01(sizeM / REFERENCE_SIZE_M);
  const curve = 1 - Math.pow(1 - n, SIZE_ESCAPE_CURVE_EXPONENT);
  return Math.min(ESCAPE_RATE_MAX, ESCAPE_AT_40M * curve);
}

// calcEffectiveEscapeRate：base 改用 calcSizeEscapeRate(sizeM)，不再传 quality 算 base
// 仍保留 escapeMultiplier、tackle 减免
```

- 鱼塘/Debug/咬钩会话**全部**改用 `calcSizeEscapeRate`
- `calcQualitySizeEscapeRate` / `QUALITY_ESCAPE_BASE` / `NEAR_MAX_ESCAPE_RATE` → `@deprecated`（运行时不用）

### 7.3 收杆（仅 sizeM）

```typescript
export function calcSizeHookDurationMs(sizeM: number): number {
  const n = clamp01(sizeM / REFERENCE_SIZE_M);
  const curve = 1 - Math.pow(1 - n, SIZE_HOOK_CURVE_EXPONENT);
  return Math.round(HOOK_MIN_MS + (HOOK_AT_40M_MS - HOOK_MIN_MS) * curve);
}

// calcHookDurationMs(quality, sizeM, speciesId) → return calcSizeHookDurationMs(sizeM);
```

- 删除 `isNearMaxSize` → `FULL_SIZE_HOOK_MS` 分支
- `FULL_SIZE_HOOK_MS` → `@deprecated`（= HOOK_AT_40M_MS）

### 7.4 速查表注释（写入 shared/fishing.ts）

```
// 体长脱钩/收杆速查（curve = 1-(1-n)^1.8, n=sizeM/40）
// sizeM   脱钩      收杆
// 0.10m   0.44%    ~34s
// 1.00m   4.4%     ~5.4min
// 10.0m   ~39%     ~55min
// 40.0m   98.5%    2h
```

### 7.5 验收

- [x] `calcSizeEscapeRate(40) === 0.985`
- [x] `calcSizeHookDurationMs(40) === 7200000`
- [x] 同 sizeM 不同 quality 脱钩/收杆相同
- [x] `scripts/a0-verify.ts` 更新断言

---

## 禁止事项（补充）

- 脱钩/收杆运行时不得使用 quality、speciesId
- 不得恢复品质脱钩表或近满硬编码

（同前版）`FISH_BITE_CHECK_MS = 300_000`；迁移；`getBiteCheckMs` 下限 300000；文案改 300s。

---

## 任务 2 — 鱼群定期补充

```typescript
// shared/pondEcology.ts
export const POND_SUPPLEMENT_CHECK_MS = 15 * 60 * 1000;
export const POND_SUPPLEMENT_TARGET_RATIO = 1.0;
export const POND_SUPPLEMENT_BATCH_RATIO = 0.50;
export const POND_SUPPLEMENT_BATCH_MAX = 12;
```

```typescript
// shared/fishing.ts — 绝对幼年体长
export const JUVENILE_SIZE_M_MIN = 0.08;
export const JUVENILE_SIZE_M_MAX = 0.20;

export function rollJuvenileSize(quality: FishQuality, species: FishSpecies): number {
  const maxSize = getQualityMaxSize(quality, species);
  const raw = JUVENILE_SIZE_M_MIN + Math.random() * (JUVENILE_SIZE_M_MAX - JUVENILE_SIZE_M_MIN);
  return round2(Math.max(MIN_FISH_SIZE_M, Math.min(raw, maxSize * 0.95)));
}
```

- 迁移 `pond_state.last_supplement_at`
- 实现 `trySupplement` / `createSupplementFish`
- **`seedPond` / `createFish` 改用 `rollJuvenileSize`**，废弃鱼塘内 `rollInitialSize`

---

## 任务 3 — 移除繁殖机制

- **删除** `tryBreed` 函数及 `tickPondEcology` 内调用
- `shared/pondEcology.ts`：`POND_BREED_CHANCE_PER_TICK` 标 `@deprecated`
- `createFish`：移除 `parentQualities` / `rollFishQualityWithParents` 参数路径
- 确认无其他入口调用繁殖

---

## 任务 4 — 脱钩成长（当前体长 ×1.02）

```typescript
export const ESCAPE_GROWTH_BONUS_RATIO = 0.02;

export function calcEscapeGrowthSize(
  quality: FishQuality,
  species: FishSpecies,
  currentSizeM: number,
): number {
  const maxSize = getQualityMaxSize(quality, species);
  return round2(Math.min(maxSize, currentSizeM * (1 + ESCAPE_GROWTH_BONUS_RATIO)));
}
```

- `applyEscapeGrowthBonus(fishId)` in `pondEcology.ts`
- 调用点：`fishingStateMachine` / `fishingSession` / Bot 脱钩路径
- **验收**：1.00m → 1.02m

---

## 任务 5 — 绝对长度生长曲线

```typescript
export const GROWTH_CURVE_DAYS = 7;
export const GROWTH_CURVE_CEILING_M = 40;
export const GROWTH_CURVE_EXPONENT = 1.8;
export const GROWTH_DAYS = 7; // @deprecated alias

export function calcStandardLengthAtAge(ageDays: number): number {
  const progress = clamp01(ageDays / GROWTH_CURVE_DAYS);
  return round2(GROWTH_CURVE_CEILING_M * (1 - Math.pow(1 - progress, GROWTH_CURVE_EXPONENT)));
}

export function growFishSizeV2(
  quality: FishQuality,
  species: FishSpecies,
  currentSizeM: number,
  birthSizeM: number,
  bornAt: number,
  now = Date.now(),
): number {
  const maxSize = getQualityMaxSize(quality, species);
  const ageDays = Math.max(0, (now - bornAt) / 86_400_000);
  const standardL = calcStandardLengthAtAge(ageDays);
  const targetSize = Math.min(maxSize, Math.max(birthSizeM, standardL));
  return round2(Math.max(currentSizeM, targetSize));
}
```

- **不要**实现 `growth_multiplier` 列
- 迁移：可选对所有 `pond_fish` 重算 `size_m`
- **验收**：`calcStandardLengthAtAge(7) === 40`；gray 鱼 ~41min 触顶 0.3m

---

## 任务 6 — 文档与验证

- `scripts/a0-verify.ts` 覆盖上述断言
- `CHANGELOG.md` 更新为待开发/已实现

---

## 禁止事项

- 不要修改 `QUALITY_BITE_BASE` / `QUALITY_ESCAPE_BASE`
- 不要保留 `tryBreed`
- 不要用品质上限% 做幼年尺寸
- 脱钩不要用「剩余空间 ×2%」
- 不要加 `growth_multiplier`

---

## 自测清单

```
[x] npx tsx scripts/a0-verify.ts
[x] 启动日志 bite check interval = 300000ms
[x] 15min 补充；幼年鱼 0.08~0.20m
[x] 无繁殖日志
[x] 脱钩 1m → 1.02m
[x] 7天 L(t)=40m；gray 早触顶
```

---

## 提交建议

```
feat(ecology): v0.3.1 bite 300s, supplement-only, absolute growth curve, escape x1.02
```
