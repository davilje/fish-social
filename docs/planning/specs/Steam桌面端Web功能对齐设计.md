# Steam 桌面端 Web 功能对齐设计

## 元信息

| 字段 | 内容 |
|------|------|
| 功能名称 | Steam 桌面端 Web 功能对齐设计 |
| 编号 | **STEAM-DESKTOP-07E-DESIGN** |
| 负责人 | 主 Agent / 制作人 |
| 状态 | **已确认** |
| 目标版本 | v1.0-steam-desktop |
| 关联需求 | `STEAM-DESKTOP-07E`、`STEAM-DESKTOP-01` |
| Web 基准 | `mobile/components/`、`mobile/lib/`、`docs/planning/product/v0.1.0-功能全景.md` |

## 1. 目标与原则

Steam 版复刻 Web 端的功能和数据语义，但不逐个平移移动端页面。

- Web 端是功能、字段、权限和服务端交互的基准。
- Steam 版采用 `1280×720` 主窗口、底部横向按钮和桌面弹窗布局。
- 鱼塘场景、自己的猫和同塘玩家仍由 07B/07C/07G 承载；07E 负责功能面板和弹窗。
- 不新增第二套业务规则、库存计算、好友权限或聊天协议。
- 弹窗打开、关闭、切换不得触发 `leave_pond`，不得重建鱼塘会话。

## 2. Web 到 Steam 功能映射

| Web 实现 | Steam 承载方式 | 07E 范围 | 优先级 |
|---|---|---:|---:|
| `PondSocialPanel` 在线钓友 | Overlay/主窗口社交弹窗或侧栏 | 是 | P0 |
| `ChatPanel` 鱼塘聊天 | 社交弹窗中的聊天页签 | 是 | P0 |
| Web 好友列表与请求 | 好友/邀请弹窗 | 是 | P0 |
| Web 私聊会话 | 好友弹窗中的私聊页签 | 是 | P0 |
| `BackpackModal` | 鱼获/背包桌面弹窗 | 是 | P0 |
| `CodexModal` | 图鉴桌面弹窗 | 是 | P1 |
| Web 窗口/通知配置 | 设置弹窗 | 是 | P0 |
| 动态墙、好友 Feed | 后续桌面社交需求 | 否 | 后置 |
| 排行榜、商店 | 后续桌面功能需求 | 否 | 后置 |

## 3. 弹窗功能规格

### 3.1 好友与聊天

#### 鱼塘社交

- 显示在线钓友数量、昵称、头像和基础钓鱼状态。
- 点击用户可打开资料摘要，不离开当前鱼塘。
- 显示鱼塘公共聊天消息和系统公告。
- 支持输入、发送和发送中状态；鱼塘聊天文本上限为 200 字。
- 空消息、发送失败、断线和重新连接必须有明确提示。

#### 好友与邀请

- 显示好友列表和在线状态。
- 显示待处理好友请求，支持接受、拒绝和移除好友。
- 支持邀请好友进入当前鱼塘。
- 好友权限继续由服务端判断，客户端不得绕过好友关系。

#### 私聊

- 显示好友会话列表、最近消息和当前会话。
- 支持加载历史消息、发送私聊和接收实时 `dm_message`。
- 私聊仅允许好友之间发送，文本上限为 300 字。

### 3.2 鱼获与背包

- 至少显示 80 个背包格子和实际占用数量。
- 每个鱼获显示鱼种图标、名称和品质。
- 选中鱼获后显示鱼种、品质、体长和出售价格。
- 显示金币余额。
- 支持出售鱼获和分享到动态；操作完成后以服务端返回结果刷新。
- 支持加载中、空背包、请求失败、权限拒绝和重试状态。
- 客户端不得自行修改库存、金币或出售价格。

### 3.3 图鉴

- 显示鱼种网格和已解锁数量。
- 已解锁鱼种显示图标、名称和详情。
- 未解锁鱼种显示锁定状态，不显示不应公开的详细结果。
- 详情至少包括：食性、基础咬钩信息、脱钩率、累计捕获、最大体型、首次捕获时间、推荐鱼饵 Top3。
- 打开时从服务端加载玩家图鉴；收到 `codex_unlocked` 后允许刷新。
- 无玩家身份、加载失败和空数据必须有明确提示。

### 3.4 设置

第一阶段只实现窗口和通知设置：

- 窗口模式、普通窗口尺寸、无边框/全屏入口。
- 关闭窗口后的托盘行为。
- 鱼咬钩通知开关。
- 好友邀请通知开关。
- 免打扰开关。

设置变更只影响本地窗口和通知表现，不修改鱼塘规则、库存或服务端状态。

## 4. Steam 桌面交互

```text
主窗口底部按钮
    ├─ 好友 / 聊天 → 社交弹窗
    ├─ 鱼获 / 背包 → 背包弹窗
    ├─ 图鉴 → 图鉴弹窗
    └─ 设置 → 设置弹窗

Overlay 右键菜单
    └─ 打开主窗口 → 显示主窗口并恢复焦点 → 打开上述弹窗
```

- 弹窗使用桌面固定面板或模态层，不跳转移动端多级页面。
- 支持关闭按钮、Esc 关闭和明确的重试入口。
- 弹窗打开时可暂时遮罩主窗口，但不得销毁 Overlay、鱼塘场景、玩家对象或 Socket 会话。
- 从 Overlay 恢复主窗口时，不得重复启动进程或重复创建鱼塘订阅。

## 5. 数据与事件对齐

### REST

| 类型 | Web 基准 | Steam 用途 |
|---|---|---|
| GET | `/api/inventory/:playerId` | 背包和鱼获 |
| GET | `/api/player/codex?playerId=...` | 图鉴 |
| GET | `/api/friends/:playerId` | 好友列表 |
| GET | `/api/friends/:playerId/requests` | 好友请求 |
| GET | `/api/dm/:playerId/:friendPlayerId` | 私聊历史 |
| POST | 现有好友请求/接受/拒绝/移除接口 | 好友管理 |
| POST | `/api/inventory/sell` | 出售鱼获 |
| `/api/players/:playerId/settings` | 现有设置接口 | 玩家设置；窗口本地设置另行保存 |

所有受保护请求必须沿用 Steam JWT，统一处理 `401/403`、超时、断线和服务端错误。

### Socket 事件

| 事件 | 用途 |
|---|---|
| `chat_message` | 鱼塘公共聊天和公告 |
| `inventory_updated` | 收鱼、出售后刷新背包 |
| `codex_unlocked` | 刷新图鉴解锁状态 |
| `friend_request` | 更新好友请求 |
| `dm_message` | 更新私聊会话 |
| `pond_user_joined/left/updated` | 更新在线钓友状态 |

弹窗不得分别创建 Socket 连接，统一消费已有桌面会话层事件。

## 6. 非目标

- 不把 Web 动态墙、好友 Feed、排行榜、商店直接并入 07E。
- 不复制 Web 的移动端路由、页面导航和响应式布局。
- 不在 Unity 客户端重写服务端鱼获、经济、好友权限或聊天规则。
- 不因弹窗切换离开鱼塘或重新认证。
- 不在本设计中替换原生 Overlay、Named Pipe 或 Unity 主窗口架构。

## 7. 验收标准

- [ ] 主窗口底部按钮可打开四类弹窗。
- [ ] 好友列表、好友请求、鱼塘聊天和私聊使用真实服务端数据。
- [ ] 鱼塘聊天限制 200 字，私聊限制 300 字。
- [ ] 背包至少显示 80 格、金币、选中鱼获详情，并能处理出售结果。
- [ ] 图鉴显示解锁状态、鱼种详情和推荐鱼饵。
- [ ] 设置可以修改窗口/托盘/通知表现。
- [ ] loading、empty、error、401/403、断线和重试状态清晰。
- [ ] `inventory_updated`、`codex_unlocked`、`friend_request`、`dm_message` 能更新对应弹窗。
- [ ] 关闭或切换弹窗不触发 `leave_pond`，不清空鱼塘和 Overlay 状态。
- [ ] Overlay 打开主窗口后，弹窗和原鱼塘会话不重复创建。

## 8. 变更记录

| 日期 | 作者 | 变更 |
|---|---|---|
| 2026-08-16 | 主 Agent | 新增 Web→Steam 功能对齐设计，明确 07E 的弹窗内容、数据基准、事件和非目标 |
