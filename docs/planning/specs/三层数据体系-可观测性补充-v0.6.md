# 三层数据体系 — 可观测性补充（v0.6）

| 状态 | **Phase 0 已实现** | 清单版本 v0.6 |
|------|--------------|---------------|
| 权威清单 | [`三层数据体系-开发需求清单.xlsx`](../reports/三层数据体系-开发需求清单.xlsx) | **36 条**（原 31 + 增补 5） |
| 生成脚本 | `scripts/build-data-platform-roadmap-xlsx.py` | 单一来源 |
| 关联 | v0.4.4 埋点表 · v0.5 架构 xlsx · 排查 SOP |

---

## 1. 文档目的

将对话中「实时排障 / 日志查看」建议**并入**正式三层数据体系，避免两套优先级并行。  
**排期、验收、分工以 xlsx 为准**；本文说明增补项与对话方案的映射关系。

---

## 2. v0.6 增补需求（5 条）

### 2.1 D-L1-10 OpenTelemetry 分布式追踪

| 字段 | 内容 |
|------|------|
| 层级 | L1 |
| 阶段 | Phase 2 |
| 优先级 | P2 |
| 对话来源 | 「Phase C — OpenTelemetry + Jaeger」 |
| 与现有关系 | 扩展 D-L1-06（correlationId），不替代 |

**目标**：`join_pond → phase_transition → bite_check → disconnect` 可见 span 树与 P95 耗时。

---

### 2.2 D-L1-11 按玩家/鱼塘动态 DEBUG 采样

| 字段 | 内容 |
|------|------|
| 层级 | L1 |
| 阶段 | Phase 1 |
| 优先级 | P1 |
| 对话来源 | 「动态日志采样 per playerId」 |
| 与现有关系 | 补充 D-L1-02（LOG_LEVEL），非重复 |

**目标**：客诉时对指定 `playerId` 临时提级 debug，TTL 30 分钟，带审计。

---

### 2.3 D-L1-12 Socket.io 全事件 Tap

| 字段 | 内容 |
|------|------|
| 层级 | L1 |
| 阶段 | Phase 1 |
| 优先级 | P2 |
| 对话来源 | 「Socket onAny 中间件」 |
| 依赖 | D-L1-11（debug 目标 100% 采样） |

**目标**：覆盖未 instrument 的 client emit；生产 1% 采样。

---

### 2.4 D-L2-13 Live Session Inspector

| 字段 | 内容 |
|------|------|
| 层级 | L2 |
| 阶段 | Phase 1 |
| 优先级 | P1 |
| 对话来源 | 「Live Session Inspector 实时订阅」 |
| 与现有关系 | 扩展 A2 `fishing-debug`（3s 快照 → 实时 SSE） |

**目标**：Admin 选定玩家后每秒推送 enriched state + 最近 metrics；与 D-L2-12 客户端上报同屏对照。

---

### 2.5 D-L2-14 独立 admin-web 运维台

| 字段 | 内容 |
|------|------|
| 层级 | L2 |
| 阶段 | Phase 1~2 |
| 优先级 | P2 |
| 对话来源 | 「Admin 从 Expo 迁出 admin-web」 |

**目标**：桌面浏览器完成 timeline 导出、fishing-debug、配置只读；mobile Admin 保留现场抽查。

---

## 3. 对话方案映射（不重复建设）

| 对话 Phase | 对应 xlsx 条目 | 备注 |
|------------|----------------|------|
| A1 Pino 统一 Logger | D-L1-01 | 一致 |
| A1 日志落盘 | D-L1-03 | 一致 |
| A3 correlationId 入 metrics | D-L1-06 | 一致 |
| A4 `/ready` 深度健康 | D-L1-08 | 已修正现状：浅 `/health` 已有 |
| A5 客户端日志上报 | D-L2-12 | 一致 |
| B Loki/Grafana | D-L1-04 + D-L2-05 + D-L1-07 | 一致 |
| B Sentry | **可选** 作为 D-L1-05 步骤 2 | 默认仍自建 `error_logs` 表 |
| C OTel | **D-L1-10**（新增） | — |
| C Live Inspector | **D-L2-13**（新增） | — |
| C 动态采样 | **D-L1-11**（新增） | — |
| C Socket Tap | **D-L1-12**（新增） | — |
| Admin SSE 本地 tail | **不采纳** | 有 D-L1-04 Loki 后不必自建 `structured_logs` |

---

## 4. 现状勘误（已写入 xlsx v0.6）

| 条目 | 旧描述 | 修正后 |
|------|--------|--------|
| D-L1-08 | 「无 /health」 | 已有浅 `/health`；缺 `/ready`、DB ping、队列深度 |
| D-L1-06 | 「Socket 有 ID」 | v0.5 R1-5 部分实现；metrics 入库与 Admin 过滤仍缺 |

---

## 5. 推荐实施顺序（实时排障最小包）

在 Phase 0 基础（D-L1-01/03/05/08）之后，若优先解决「线上盯人、查链」：

```
D-L1-06 → D-L2-06 → D-L1-11 → D-L2-13 → D-L1-04
```

全量 Phase 0~1 见 xlsx「阶段路线图」sheet。

**开发提示词**：[`data-platform-phase0-dev.prompt.md`](../prompts/data-platform-phase0-dev.prompt.md)

```
@docs/planning/prompts/data-platform-phase0-dev.prompt.md 按此实现
```

---

## 6. Phase 0 里程碑

Phase 0（D-L1-01/02/03/05/08、D-L2-01/02/03/08/11、D-L3-01）于 2026-07-11 完成实现。

| 条目 | 状态 | 说明 |
|------|------|------|
| D-L1-01 | ✅ 已实现 | pino 统一 Logger，JSON 字段契约 |
| D-L1-02 | ✅ 已实现 | error/warn/info/debug 级别策略 |
| D-L1-03 | ✅ 已实现 | 日轮转（pino-roll）、Docker stdout |
| D-L1-05 | ✅ 已实现 | error_logs 表、DB 持久化、90d 保留 |
| D-L1-08 | ✅ 已实现 | /health（liveness）+ /ready（readiness） |
| D-L2-01 | ✅ 已实现 | 事件字典 Schema + dev 校验 |
| D-L2-02 | ✅ 已实现 | metrics 90d 归档脚本 |
| D-L2-03 | ✅ 已实现 | daily_player/pond_stats 日聚合表 |
| D-L2-08 | ✅ 已实现 | 33 条埋点全绿（verify:server-observability） |
| D-L2-11 | ✅ 已实现 | SQLite 备份脚本 + 保留 7 份 |
| D-L3-01 | ✅ 已实现 | 日批流水线（compact.json + report.html） |

实现详情见 [`data-platform-phase0-dev.prompt.md`](../prompts/data-platform-phase0-dev.prompt.md) 及 commit 记录。

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-11 | Phase 0 实现完成：Logger、健康检查、错误日志持久化、日聚合、备份、日报流水线 |
| 2026-07-10 | v0.6：增补 D-L1-10~12、D-L2-13~14；对齐对话可观测性建议；勘误 L1-06/08 |
