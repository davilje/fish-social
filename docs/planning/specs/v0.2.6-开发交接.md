# v0.2.6 开发交接 — 咬钩 30s、Debug 精度、饵文案

> 策划已完成，状态：**已实现**（2026-07-06）。

---

## 元信息

| 字段 | 内容 |
|------|------|
| 目标版本 | v0.2.6 |
| 主 Spec | [分析与修复-钓鱼概率与饵文案.md](./分析与修复-钓鱼概率与饵文案.md) |
| 范围 | **仅 P0**（无 tick 概率公式调整） |
| 策划日期 | 2026-07-06 |

---

## 产品决策（必读）

1. **实际咬钩必须 30 秒判定一次** — 改服务端 `FISH_BITE_CHECK_MS`，不是只改 Debug 文案。
2. **Debug 基础咬钩率** — 显示正确数值，**百分比保留 2 位小数**（如 2.40%）。
3. **本钓位 100%** — **不是 Bug**，不修改 `min(1, Σeffective)`；间隔与文案修好后 **如实展示** 即可。

---

## 交接提示词（复制给开发 Agent）

```
你是 Fish Social 开发 Agent。只写代码，策划已确认。

## 必读
1. docs/planning/specs/v0.2.6-开发交接.md
2. docs/planning/specs/分析与修复-钓鱼概率与饵文案.md

## 任务 1 — 服务端：咬钩间隔 30 秒（必做）
- 强制 game_config FISH_BITE_CHECK_MS = 30000（迁移/启动 upsert，覆盖 5000）
- getBiteCheckMs() 运行时确保 30000
- 玩家 + Bot 咬钩循环均使用该值
- 启动 log 打印实际 interval ms
- 验收：waiting 阶段每 30s 判定，非 5s

## 任务 2 — Debug 数值与间隔文案（必做）
- fishingDebug.ts：fishBiteRate / baitBonus / spotBiteRate / tickBiteChance 勿 round2 抹平；下发足够精度
- AdminPondFishDebugGrid：
  - formatProb: (rate*100).toFixed(2)+'%'
  - 硬编码 /30s → 使用 data.constants.checkMs 动态秒数
- 满尺寸鱼 baseBite 显示 3.00%

## 任务 3 — 饵文案统一（必做）
- shared/fishing.ts: formatBiteRatePct(rate) → (rate*100).toFixed(2)+'%'
- ShopModal: 基础加成 + 食性偏好（均 %）；禁止 +0.06 小数
- CodexModal: 推荐饵同 format
- Debug 鱼格复用 formatBiteRatePct

## 明确不做
- 不改 calcTickBiteProbability / min(1,Σeffective)
- 不做 tick 归一化、不调 biteWeight
- 不改 hookDurationMs、状态机、baitBiteBonus 结构

## 验收
- [ ] 配置 + 日志 + 体感：30s 判定
- [ ] Debug 基础咬钩有 2.40%/1.60% 等差异，非清一色 2.00%
- [ ] 间隔文案与 checkMs 一致
- [ ] tick 100% 可显示，本单不要求降低
- [ ] 活虾+肉食：商店 4%+14%，Debug/图鉴 18.00%

## 完成后
- [ ] 自检主 Spec §6
- [ ] CHANGELOG + spec 状态→已实现
```
